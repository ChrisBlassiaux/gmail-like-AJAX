<% if email.read %>
    <%= link_to email.object, email_path(email), id: email.id, class:"list-group-item list-group-item-action", remote: true %>
<% else %>
    <%= link_to email.object, email_path(email), id: email.id, class:"list-group-item list-group-item-action text-light bg-secondary", remote: true %>
<% end %>
